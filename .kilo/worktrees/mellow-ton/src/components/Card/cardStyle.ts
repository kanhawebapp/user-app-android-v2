import { StyleSheet } from "react-native";

export const cardStyles = StyleSheet.create({
  card: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  disabled: {
    opacity: 0.5,
  },
});